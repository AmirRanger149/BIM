# 🏠 راهنمای استفاده از 3D Model Viewer

## 📋 خلاصه

سیستم BIM اکنون قادر است مدل‌های سه‌بعدی (3D) را نمایش دهد. پروژه‌ها می‌تواند مدل‌های GLB، GLTF، و OBJ را شامل باشند.

---

## ✨ ویژگی‌ها

✅ **نمایش مدل‌های 3D** - GLB, GLTF, OBJ  
✅ **تعامل کامل** - چرخش، زوم، پن  
✅ **دوربین خودکار** - تطابق خودکار با مدل  
✅ **دانلود اسکرین‌شات** - ذخیره تصویری از مدل  
✅ **تمام‌صفحه** - نمایش تمام صفحه  
✅ **بارگذاری خودکار** - شناخت خودکار فرمت  

---

## 🚀 شروع سریع

### 1️⃣ داشتن مدل 3D

فایل‌های نمونه در `/backend/uploads/`:
- `house_model.glb` (5.9 KB) - خانه رنگی کامل
- `building_complex.glb` (1.9 KB) - ساختمان چندطبقه
- `building_sample.glb` (976 B) - مکعب ساده

### 2️⃣ اضافه کردن مدل به پروژه

از طریق **Admin Panel**:

```
Admin → گالری → افزودن پروژه جدید
└─ بخش "📦 مدل 3D"
   ├─ انتخاب فایل یا اسلاشی URL
   └─ نوع مدل: تشخیص خودکار/GLTF/GLB/OBJ
```

### 3️⃣ نمایش مدل

صفحهٔ پروژه خودکار مدل را نمایش می‌دهد:

```
http://localhost:5173/project/{id}
```

---

## 📊 API Endpoints

### دریافت پروژه

```bash
GET /api/gallery/{id}
```

Response:
```json
{
  "data": {
    "id": 3,
    "title": "نمونه: خانهٔ 3D",
    "model_url": "/uploads/house_model.glb",
    "model_type": "glb",
    "image": "...",
    "description": "..."
  }
}
```

### آپلود مدل

```bash
POST /api/admin/upload
Content-Type: multipart/form-data

file: <file.glb>
```

Response:
```json
{
  "success": true,
  "filename": "house_model.glb",
  "stored_filename": "abc123_house_model.glb",
  "url": "/uploads/abc123_house_model.glb",
  "size": 5932,
  "content_type": "model/gltf-binary"
}
```

---

## 🎮 کنترل‌های کاربر

| کنترل | عملکرد |
|-------|--------|
| **چپ‌کلیک + بکش** | چرخش دوربین |
| **اسکرول ماوس** | بزرگنمایی / کوچکنمایی |
| **راست‌کلیک + بکش** | حرکت (Pan) |
| **🔄 دکمه** | بازنشانی دوربین |
| **⛶ دکمه** | تمام‌صفحه |
| **⬇️ دکمه** | دانلود اسکرین‌شات |

---

## 🔧 استفاده در کامپوننت‌ها

### BasicUsage

```vue
<template>
  <Viewer3D
    :modelUrl="project.model_url"
    :modelType="project.model_type || 'auto'"
    :autoRotate="true"
    backgroundColor="#f5f5f5"
  />
</template>

<script setup>
import Viewer3D from '@/components/Viewer3D.vue'
import { ref } from 'vue'

const project = ref({
  model_url: '/uploads/house_model.glb',
  model_type: 'glb'
})
</script>
```

### Advanced Usage

```vue
<Viewer3D
  :modelUrl="getModelUrl(project)"
  :modelType="detectModelType(project)"
  :autoRotate="true"
  backgroundColor="#ffffff"
/>
```

---

## 📦 فرمت‌های پشتیبانی شده

### GLTF (GL Transmission Format)

✅ **GLB** - بایناری، فشرده‌شدهٔ  
✅ **GLTF** - متنی، برای توسعه‌ی وب  

**کاربرد**: معماری، صنعتی، حرفه‌ای

```
نام‌فایل: model.glb
اندازه: ~5-50 MB
تشخیص: خودکار
```

### OBJ (Wavefront)

✅ **OBJ** - فرمت ساده و سازگار  

**کاربرد**: مدل‌های ساده، هندسی

```
نام‌فایل: model.obj
اندازه: ~1-100 MB
تشخیص: خودکار
```

---

## 🐛 مشکل‌گشایی

### مدل بارگذاری نمی‌شود

1. **بررسی URL**
   ```javascript
   // Console میں
   console.log('Model URL:', project.model_url)
   ```

2. **بررسی فایل**
   ```bash
   ls -lh /workspaces/BIM/backend/uploads/
   ```

3. **بررسی فرمت**
   ```bash
   file /path/to/model.glb
   ```

### دوربین شناخته نمی‌شود

- مطمئن شوید مدل هندسهٔ درست دارد
- تابع `fitCameraToModel` باید Box3 محاسبه کند

### Performance کند است

- کاهش اندازهٔ فایل GLB
- استفاده از LOD (Level of Detail)
- بررسی تعداد vertices

---

## 📈 آمار

```
Backend URL: http://localhost:8000
Frontend URL: http://localhost:5173

مدل‌های موجود:
├─ 3 فایل GLB
├─ ~10 KB در مجموع
└─ 1 پروژهٔ فعال

Database:
├─ GalleryItem.model_url ✅
└─ GalleryItem.model_type ✅
```

---

## 🚀 Development

### Build کردن

```bash
npm run build
```

### Development Server

```bash
# Backend
cd backend && python main.py

# Frontend
npm run dev
```

### Test کردن

```bash
python test_3d_system.py
```

---

## 📚 منابع اضافی

- [Three.js Documentation](https://threejs.org/docs/)
- [glTF Specification](https://github.com/KhronosGroup/glTF)
- [OBJ Format](https://en.wikipedia.org/wiki/Wavefront_.obj_file)

---

## 💡 نکات مهم

1. **URL‌های مطلق** - سیستم خودکار URL‌های نسبی را تبدیل می‌کند
2. **CORS** - فایل‌ها باید از یک منبع قابل دسترسی باشند
3. **حجم فایل** - حداکثر 100MB برای آپلود
4. **شناخت نوع** - با توجه به پسوند فایل

---

## ✅ Checklist

- [ ] مدل‌های 3D آپلود شدند
- [ ] پروژه‌ها مدل را ارجاع می‌دهند
- [ ] صفحهٔ جزئیات مدل را نمایش می‌دهد
- [ ] کنترل‌ها درست کار می‌کنند
- [ ] دانلود اسکرین‌شات کار می‌کند

---

**آخرین به‌روزرسانی:** December 18, 2025  
**نسخه:** 1.0.0  
**وضعیت:** ✅ Production Ready
